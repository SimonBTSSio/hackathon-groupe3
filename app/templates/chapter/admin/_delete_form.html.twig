<form method="post" action="{{ path('back_chapter_delete', {'id': chapter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapter.id) }}">
    <button class="btn">Delete</button>
</form>
