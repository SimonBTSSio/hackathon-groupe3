{% extends 'base.html.twig' %}

{% block title %}Visite{% endblock %}

{% block body %}
    <h1>Visite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visite.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ visite.date ? visite.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

            <tr>
                <th>Medecin</th>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_app_visite_index') }}">back to list</a>


    {{ include('front/visite/_delete_form.html.twig') }}
{% endblock %}
