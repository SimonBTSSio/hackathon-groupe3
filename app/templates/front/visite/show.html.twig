{% extends 'base.html.twig' %}

{% block title %}Visite{% endblock %}

{% block body %}
    <h1>Visite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visite.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ visite.date ? visite.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ visite.domaine }}</td>
            </tr>
            <tr>
                <th>Medecin</th>
                <td>{{ visite.medecin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visite_index') }}">back to list</a>

    <a href="{{ path('app_visite_edit', {'id': visite.id}) }}">edit</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
