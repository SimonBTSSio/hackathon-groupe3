{% extends 'base-back.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
    <h1 class="text-3xl text-black flex flex-wrap -mx-3 px-3 gap-2">Edit Question</h1>

    {{ include('question/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="text-sm text-black flex flex-wrap -mx-3 px-3 gap-2" href="{{ path('back_quizz_show', {'id' : question.quizz.id}) }}">Retour</a>

{% endblock %}
