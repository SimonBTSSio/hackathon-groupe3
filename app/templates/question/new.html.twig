{% extends 'base-back.html.twig' %}

{% block title %}Nouvelle question{% endblock %}

{% block body %}
    <h1 class="text-3xl text-black flex flex-wrap -mx-3 px-3 gap-2">Ajout nouvelle question</h1>

    {{ include('question/_form.html.twig') }}

    <a class="text-sm text-black flex flex-wrap -mx-3 px-3 gap-2" href="{{ path('back_quizz_show', {'id' : quizz.id}) }}">Retour</a>
{% endblock %}
